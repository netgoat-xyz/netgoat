<!DOCTYPE html>
<html lang="en" class="h-full w-full">
  <head>
    <meta charset="UTF-8" />
    <title>NetGoat Access</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cal+Sans&family=Inter:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <style>
      body, html {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>
  <body class="h-full w-full m-0 p-0 overflow-hidden">
    <div class="relative h-screen w-screen">
      <!-- Background Image -->
      <img
        src="/assets/img/bg_dark.jpg"
        alt="bg"
        class="absolute inset-0 h-full w-full object-cover"
      />

      <!-- Overlay -->
      <div class="absolute inset-0 bg-white/10 backdrop-blur-lg z-10"></div>

      <!-- Form -->
      <div class="absolute inset-0 z-20 flex flex-col items-center justify-center px-4">
        <!-- Logo/Icon -->
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-10 h-10 text-white mb-4"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>

        <!-- Heading -->
        <h1 class="text-white text-4xl font-extrabold mb-6">NetGoat Access</h1>

        <!-- Form Fields -->
        <form class="w-full max-w-sm flex flex-col space-y-3">
          <div>
            <label class="text-white text-sm mb-1 block" for="email">E-Mail</label>
            <input
              id="email"
              type="email"
              class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-white/60 backdrop-blur-sm focus:outline-none"
              placeholder="you@example.com"
            />
          </div>
          <div>
            <label class="text-white text-sm mb-1 block" for="password">Password</label>
            <input
              id="password"
              type="password"
              class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-white/60 backdrop-blur-sm focus:outline-none"
              placeholder="••••••••"
            />
          </div>
          <button
            type="submit"
            class="w-full py-2 rounded-md bg-white/20 text-white font-semibold hover:bg-white/30 transition"
          >
            Login
          </button>
        </form>
      </div>

      <!-- Footer -->
      <footer class="absolute bottom-4 w-full text-center text-white text-sm opacity-80 z-20">
        Cloudable NetGoat© 2025
      </footer>
    </div>
  </body>
</html>
